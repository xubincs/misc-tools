#!/usr/bin/python3

import subprocess
import time

from gi.repository import Gtk

if __name__ == '__main__':
    time.sleep(5)
    subprocess.call(['xinput', 'set-prop', '13', 'Device Enabled', '0'])
